/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: process.env.SITE_URL || 'https://animetopx.vercel.app', // Cambia por tu URL
    generateRobotsTxt: true, // (opcional)
    // Otras configuraciones opcionales
  };
  